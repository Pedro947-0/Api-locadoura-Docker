spring.application.name=locadora-veiculos


# Banco local.

#spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5434/Locadora}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Ko947}
#spring.datasource.driver-class-name=org.postgresql.Driver

# Banco virtual render..

# NOTE: Render Postgres instances normally require SSL. Add sslmode=require to force TLS.
spring.datasource.url=jdbc:postgresql://dpg-d5jfem14tr6s73aprp8g-a.oregon-postgres.render.com/locadora_nlac?sslmode=require
spring.datasource.username=ko947
spring.datasource.password=wVa0DeeSL173rPi8MqnkRnLWbQQxyNtS
spring.datasource.driver-class-name=org.postgresql.Driver

# Optional Hikari / datasource tuning and health checks
# Validate connections with a simple query and reduce pool size during development
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.connection-timeout=30000
# If you prefer the app to not fail fast when DB is unreachable during startup set to 0
# spring.datasource.hikari.initialization-fail-timeout=0

# Control SQL initialization (data.sql/schema.sql). Set to 'never' when using a remote DB
# to avoid the initializer treating an external DB as embedded and attempting to run scripts.
# Default behavior checks for embedded databases; setting to 'never' disables automatic init.
spring.sql.init.mode=never
spring.sql.init.platform=postgres

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
